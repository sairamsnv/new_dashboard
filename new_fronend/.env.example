# Django WMS backend (no trailing slash). Required for production builds.
# Local dev: copy to `.env` or rely on `.env.development` / dev fallback in `src/lib/wmsApi.ts`.
VITE_WMS_API_BASE_URL=http://127.0.0.1:8000
